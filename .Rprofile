# Set the R package library
.libPaths("C:/R/win-library")

# Write message to R console on session start up.
message("Using library: ", .libPaths()[1])

# Scientific penalty option.
options(scipen = 999)

# Set the working directory
#setwd("C:/R")

# Set shared functions to use
functions <- "C:/R/Shared Functions/99_Functions.R"

#FOR R PROJECTS: Place this file in the same project folder as the .Rproj file
